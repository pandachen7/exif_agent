# EXIF Agent Configuration File
# 設定檔 - 請依照實際需求修改

[Path]
# 多媒體資料夾路徑
PathInput =

# 輸出路徑
PathOutput = ./output

[Processing]
# 時間間隔 (分鐘) - 用於計算有效照片數
# 預設: 30 分鐘
DefaultTimeInterval = 30

# OCR 引擎選擇
# 可選: paddle, tesseract
OCREngine = paddle

# 是否啟用偵錯模式
DebugMode = False

[Database]
# Access DB 檔案名稱
AccessDBName = exif_data.accdb

# Excel 檔案名稱
ExcelFileName = exif_data.xlsx

# CSV 檔案名稱
CSVFileName = exif_data.csv
